// src/queries/getDatoCmsToken.ts
// Stubbed token fetcher — disables DatoCMS lookup

export const getDatoCmsToken = (): string => {
  console.warn("DatoCMS token disabled — returning empty string.");
  return "";
};
